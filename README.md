# Budget App - Frontend

### Technologies Used
- React
- TypeScript
- Material UI
- React Query

### How to Run
1. Clone the project
2. Run `npm install` to install node dependencies
3. Run `npm run dev` to run development build

